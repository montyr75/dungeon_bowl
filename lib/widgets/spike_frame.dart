import 'package:flutter/material.dart';

class SpikeFrame extends StatelessWidget {
  const SpikeFrame({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
